extends _partial/layout.pug

block title
	title
		= __("category")
		- if (page.category) {
			|: #{page.category}
		- }

block content
	div
		ul
			- site.categories.forEach(function(item){
				li
					h2
						a(href=url_for(item.path))
							span
								= item.name
			- })

